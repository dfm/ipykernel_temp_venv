# Temporary environments for IPython kernels

## Usage

Istall the package:

```bash
python -m pip install ipykernel_temp_venv
```

Install the kernelspec:

```bash
python -m ipykernel_temp_venv --sys-prefix
```
